#include "libft.h"
#include <stdio.h>

int main(void)
{
    char c = ;
    if (ft_isascii(c))
    {
        printf("%c est un caractère ascii\n", c);
    }
    else
    {
        printf("%c n'est pas un caractère ascii RIP.\n", c);
    }
return(0);
}